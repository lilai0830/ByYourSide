# AI 眼镜出行辅助 Agent —— 后端依赖
# 直接从业务代码 import 的第三方包（其余为传递依赖，pip 会自动安装）。
# 版本锁定自当前可运行的 .venv，保证复现一致。

fastapi==0.141.1        # 后端 HTTP / WebSocket 服务（server.py）
uvicorn==0.52.1         # ASGI 服务器，运行 backend/server.py
langgraph==1.2.10       # 编排状态机（graph.py: StateGraph / MemorySaver / interrupt）
